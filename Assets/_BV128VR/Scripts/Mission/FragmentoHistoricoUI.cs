using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FragmentoHistoricoUI : MonoBehaviour
{
    public Text TxtProntos;

    public GameObject painelFragmentos;
    public Text txt_titulo, txt_ano, txt_descricao, txt_contexto;
    public RawImage imagem;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
